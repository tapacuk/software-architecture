{
  "name": "software-architecture",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^25.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "start": "bunx tsc && bunx serve .",
    "build": "bun build ./src/ui/app.ts --outdir ./dist",
    "dev": "bun build ./src/ui/app.ts --outdir ./dist && bunx tsc && bunx serve ."
  },
  "dependencies": {
    "readline": "^1.3.0"
  }
}
